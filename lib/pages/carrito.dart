import 'package:flutter/material.dart';

class CarritoPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("TODO: Carrito Page"),
    );
  }
}